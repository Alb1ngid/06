git init -- ининциализация проекта
git remote add origin -- привязка репозитория с проектом
файл .gitignore -- скрытие файлов

git add --добавление файлов в репозиторий
git commit -m 'результат' --- сохранение изменений
git push origin master/main --- загрузка вашего проекта в ваш репозиторий


git rm --cached : удаление файлов с репозитория
git rm --cached -r : удаление папок\директорий с репозитория